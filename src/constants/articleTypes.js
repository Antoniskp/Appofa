/**
 * Article type constants for backend use
 */

// Valid article types that match the database ENUM
const ARTICLE_TYPES = ['personal', 'articles', 'news', 'video'];

module.exports = {
  ARTICLE_TYPES
};
